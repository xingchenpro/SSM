<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--别名，可以用这个别名代替这个类-->
    <typeAliases>
        <typeAlias type="com.learn.mybatis.pojo.User" alias="user" ></typeAlias>
    </typeAliases>
    <!--数据库环境-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"></transactionManager>
            <!--POOLED代表采用内部提供的连接池方式-->
            <dataSource type="POOLED">
                <property name="driver" value="com.microsoft.sqlserver.jdbc.SQLServerDriver"/>
                <property name="url" value="jdbc:sqlserver://127.0.0.1:1433;DatabaseName=ssm"/>
                <property name="username" value="sa"/>
                <property name="password" value="abc123456*"/>
            </dataSource>
        </environment>
    </environments>
    <!--映射文件-->
    <mappers>
        <mapper resource="com/learn/mybatis/mapper/UserMapper.xml"></mapper>
    </mappers>
</configuration>